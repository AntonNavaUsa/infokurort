# 📊 Система логирования AI

Детальное логирование RAG системы и OpenAI запросов для анализа токенов и стоимости.

## 🎯 Что логируется

### 1. Инициализация RAG
- Количество загруженных файлов
- Количество созданных chunks
- Оценка токенов для embeddings
- Стоимость создания embeddings

### 2. Поиск в RAG (каждый запрос)
- Запрос пользователя
- Определённая категория
- Найденные документы (source, category, размер)
- Оценка токенов контекста

### 3. Запросы к OpenAI
- Используемая модель
- Разбивка токенов:
  - Системный промпт
  - RAG контекст  
  - Сообщения пользователя
  - ИТОГО вход
  - ИТОГО выход
- Разбивка стоимости:
  - Input cost
  - Output cost
  - Total cost

### 4. Сводка по сессии
- Общее количество запросов
- Суммарные токены (вход/выход)
- Общая стоимость
- Средняя стоимость за запрос

## 🖥️ Использование

### В браузере (Dev Mode)

При запуске в development режиме появляется кнопка **"📊 AI Логи"** в правом нижнем углу.

**Панель показывает:**
- Статистику инициализации RAG
- Общую сводку по сессии
- Последние 5 запросов с детализацией
- Кнопки действий:
  - 📄 **Консоль** - вывести сводку в console.log
  - 💾 **Скачать** - скачать JSON файл с логами
  - ✅ **Сохранить** - сохранить в localStorage + скачать

### В консоли браузера

```javascript
// Доступен глобальный объект aiLogger
aiLogger.printSummary()        // Вывести сводку
aiLogger.downloadSessionLog()  // Скачать JSON
aiLogger.saveSession()         // Сохранить в localStorage + скачать
aiLogger.getSession()          // Получить объект сессии
```

### Пример вывода консоли

```
📊 СВОДКА ПО СЕССИИ
============================================================
🆔 ID: session-1703612345678
🕐 Начало: 26.12.2025, 17:52:15
📝 Запросов: 5

🔄 ИНИЦИАЛИЗАЦИЯ RAG:
  Файлов загружено: 7
  Chunks создано: 95
  Токенов (embeddings): 12,450
  Стоимость: $0.000249

💬 ЗАПРОСЫ:

  Запрос #1:
    Вопрос: Расскажи про Розу Хутор
    Модель: gpt-4o-mini
    RAG chunks: 3
    Токены вход: 1,234
    Токены выход: 456
    Стоимость: $0.000461

  ... (остальные запросы)

💰 ИТОГО:
  Токенов вход: 6,234
  Токенов выход: 2,145
  ОБЩАЯ СТОИМОСТЬ: $0.002345
  Стоимость за запрос: $0.000469
============================================================
```

## 📁 Структура лог-файла

```json
{
  "sessionId": "session-1703612345678",
  "startTime": "2025-12-26T14:52:15.123Z",
  "initialization": {
    "timestamp": "2025-12-26T14:52:15.456Z",
    "filesLoaded": 7,
    "totalChunks": 95,
    "embeddingTokensEstimate": 12450,
    "embeddingCostEstimate": 0.000249
  },
  "requests": [
    {
      "requestId": 1,
      "timestamp": "2025-12-26T14:52:20.789Z",
      "userMessage": "Расскажи про Розу Хутор",
      "ragSearch": {
        "timestamp": "2025-12-26T14:52:20.800Z",
        "query": "Расскажи про Розу Хутор",
        "category": "resorts",
        "chunksFound": 3,
        "chunks": [
          {
            "source": "resorts/roza-hutor.md",
            "category": "resorts",
            "contentLength": 487,
            "preview": "# Роза Хутор\n\n## Общая информация\n\nРоза Хутор — крупнейший и самый современный..."
          }
        ],
        "contextTokensEstimate": 365
      },
      "openaiRequest": {
        "timestamp": "2025-12-26T14:52:21.234Z",
        "model": "gpt-4o-mini",
        "systemPromptTokens": 150,
        "ragContextTokens": 365,
        "messagesTokens": 15,
        "totalInputTokens": 530,
        "outputTokens": 234,
        "inputCost": 0.0000795,
        "outputCost": 0.0001404,
        "totalCost": 0.0002199
      },
      "response": "Роза Хутор - это крупнейший горнолыжный курорт России..."
    }
  ],
  "totalCost": 0.002345,
  "totalTokensIn": 6234,
  "totalTokensOut": 2145
}
```

## 💰 Цены моделей (актуальные на декабрь 2025)

### GPT-4o-mini
- Input: $0.150 / 1M токенов
- Output: $0.600 / 1M токенов

### GPT-4o
- Input: $2.50 / 1M токенов  
- Output: $10.00 / 1M токенов

### text-embedding-3-small
- Input: $0.020 / 1M токенов

## 📈 Анализ стоимости

### Пример расчёта для 1 запроса:

**Входные токены (530):**
- Системный промпт: 150 токенов
- RAG контекст: 365 токенов  
- Сообщение: 15 токенов

**Выходные токены:** 234

**Стоимость:**
- Input: 530 × $0.000000150 = $0.0000795
- Output: 234 × $0.000000600 = $0.0001404
- **Total: $0.0002199** (~$0.22 за 1000 запросов)

### Сравнение с подходом "большой промпт"

| Параметр | Без RAG | С RAG | Экономия |
|----------|---------|-------|----------|
| Системный промпт | ~8000 токенов | ~150 токенов | **-98%** |
| Контекст | Весь статичный | Динамический 365 | Релевантнее |
| Токены вход | ~10,000 | ~530 | **-95%** |
| Стоимость/запрос | $0.0024 | $0.00022 | **-90%** |

## 🔍 Диагностика проблем

### Если стоимость высокая:

1. **Проверьте RAG инициализацию**
   - Инициализируется ли RAG каждый раз? (должно быть 1 раз)
   - Сколько токенов уходит на embeddings?

2. **Проверьте размер контекста**
   - Сколько chunks возвращается? (должно быть 3-4)
   - Какой размер каждого chunk? (должно быть ~500 символов)

3. **Проверьте токены промпта**
   - Системный промпт должен быть ~150 токенов
   - RAG контекст должен быть ~300-500 токенов

4. **Проверьте модель**
   - Используется ли gpt-4o-mini? (не gpt-4/gpt-4o!)

### Как найти проблему:

1. Откройте панель логов
2. Посмотрите детализацию последнего запроса
3. Найдите аномально большие значения:
   - systemPromptTokens > 200 → промпт слишком большой
   - ragContextTokens > 600 → слишком много chunks или большие chunks
   - model != 'gpt-4o-mini' → используется дорогая модель

## 💡 Советы по оптимизации

1. **Уменьшите chunk size** (src/lib/ai/rag.ts):
   ```typescript
   chunkSize: 500,  // Вместо 1000
   ```

2. **Уменьшите количество chunks** (src/lib/ai/openai.ts):
   ```typescript
   const relevantDocs = await searchKnowledge(lastUserMessage, 2); // Вместо 3
   ```

3. **Оптимизируйте системный промпт** (src/lib/ai/prompts.ts):
   - Удалите лишние примеры
   - Сократите инструкции
   - Уберите повторы

4. **Используйте категории** (src/lib/ai/rag.ts):
   - Детектор категорий ускоряет поиск
   - Фильтр по категории даёт более точный контекст

## 📦 Экспорт логов

Лог-файлы можно:
- Скачать через кнопку "💾 Скачать" (JSON файл)
- Сохранить в localStorage через кнопку "✅ Сохранить"
- Проанализировать в Excel/Google Sheets (конвертировать JSON)
- Отправить в аналитику (parse JSON, отправить метрики)

## 🛠️ Интеграция с аналитикой

```typescript
import { aiLogger } from '@/lib/ai/logger';

// После каждого запроса
const session = aiLogger.getSession();
const lastRequest = session?.requests[session.requests.length - 1];

// Отправить в Google Analytics / Mixpanel / etc
analytics.track('ai_request', {
  tokens_in: lastRequest.openaiRequest.totalInputTokens,
  tokens_out: lastRequest.openaiRequest.outputTokens,
  cost: lastRequest.openaiRequest.totalCost,
  model: lastRequest.openaiRequest.model,
  rag_chunks: lastRequest.ragSearch.chunksFound,
});
```

## 🚀 Production

В production режиме:
- UI панель **НЕ отображается** (только в DEV)
- `aiLogger` **доступен** в коде для метрик
- localStorage **работает** (можно сохранять для отладки)
- Console logs **можно отключить** (добавить флаг в logger.ts)
