# AI Ski Concierge - MVP 🎿

## Что реализовано

✅ **AI-помощник на базе GPT-4**
- Интеллектуальный чат для консультации по курортам
- Автоматический расчет стоимости занятий с учетом сезонов
- Сбор контактных данных для бронирования
- Сохранение заявок в localStorage

✅ **База знаний**
- Детальные описания 3 курортов (Роза Хутор, Красная Поляна, Газпром)
- 71 программа обучения с сезонными ценами
- 18 часто задаваемых вопросов

✅ **Function Calling**
- `calculate_price` - расчет стоимости с учетом дат и сезонов
- `get_resort_info` - информация о курортах
- `create_booking_draft` - создание заявки на бронирование
- `search_faq` - поиск по базе вопросов

## Как запустить

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Откройте http://localhost:8080 (или указанный порт) в браузере.

## Как использовать

### Тестирование чата

1. Откройте главную страницу
2. Нажмите на иконку чата (плавающая кнопка справа)
3. Задайте вопрос, например:
   - "Хочу научить сына кататься на лыжах, ему 7 лет"
   - "Сколько стоят индивидуальные занятия в январе?"
   - "Какой курорт лучше для новичков?"

### Просмотр заявок

В консоли браузера (F12) доступны утилиты:

```javascript
// Посмотреть все заявки
bookings.get()

// Экспортировать в JSON
bookings.export()

// Очистить все заявки
bookings.clear()
```

## Примеры диалогов

### Консультация по курортам

**Пользователь:** Какой курорт выбрать для начинающего?

**AI:** На ваше место я бы выбрал Розу Хутор - там самая развитая инфраструктура и отличные трассы для начинающих. У них есть специальная программа Progress для поэтапного обучения. Хотите узнать цены на групповые или индивидуальные занятия?

### Расчет цены

**Пользователь:** Сколько стоит индивидуальное занятие на 3 часа в январе на Красной Поляне?

**AI:** *[вызывает calculate_price]* 

На Красной Поляне индивидуальное занятие на 3 часа в январе (высокий сезон) стоит 9500₽. Если планируете кататься несколько дней, могу просчитать абонемент - выйдет выгоднее!

### Бронирование

**Пользователь:** Хочу забронировать инструктора на 15-17 января для ребенка

**AI:** Отлично! Какой курорт вас интересует? И уточните, пожалуйста, возраст ребенка и его уровень катания.

*[после сбора всех данных создает заявку через create_booking_draft]*

## Структура проекта

```
src/
├── lib/ai/
│   ├── prompts.ts       # Системный промпт для GPT-4
│   ├── openai.ts        # API wrapper для OpenAI
│   └── bookings.ts      # Утилиты для работы с заявками
├── hooks/
│   └── useAIChat.ts     # React hook для AI чата
├── data/
│   ├── resorts.ts       # Данные о курортах
│   ├── faq.ts           # Часто задаваемые вопросы
│   └── skiSchoolPricing.ts  # Цены на обучение
└── components/chat/
    └── ChatInterface.tsx    # UI компонент чата
```

## Технологии

- **AI:** OpenAI GPT-4 с Function Calling
- **Frontend:** React + TypeScript + Vite
- **UI:** shadcn/ui + Tailwind CSS
- **Storage:** localStorage (MVP)

## Следующие шаги (Roadmap)

📋 **Фаза 2: CRM интеграция** (Опционально)
- Отправка заявок на email
- Интеграция с Telegram Bot
- Dashboard для менеджеров

📋 **Фаза 3: Продвинутые функции** (Опционально)
- Календарь доступности инструкторов
- Онлайн оплата
- Отзывы и рейтинги
- RAG для ответов на сложные вопросы

## API Key

API ключ хранится в `.env.local`:

```env
VITE_OPENAI_API_KEY=sk-proj-...
```

⚠️ **Безопасность:** В production используйте backend proxy для защиты API ключа!

## Ограничения MVP

- ❌ Нет backend - все в браузере
- ❌ API ключ виден в клиенте (только для dev!)
- ❌ Нет реальной отправки заявок менеджерам
- ❌ Нет календаря и проверки доступности
- ❌ Нет профилей инструкторов

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.

---

Сделано с ❤️ для горнолыжников Сочи 🏔️
